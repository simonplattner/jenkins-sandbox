pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh "echo hello build"
      }
    }
    stage('Test') {
      steps {
        sh "echo hello build"
      }
    }
    stage('Deploy') {
      steps {
        sh "echo hello build"
      }
    }
  }
}
